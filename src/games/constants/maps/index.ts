import { IWorldMapConfig } from "@/games/types";

// type MapKey = "WelcomeMap" | "CityMap" | (string & {});

export const MAPS: Record<string, IWorldMapConfig> = {
    // WelcomeMap: WelcomeMap,
    // CityMap: CityMap,
};
