const TILE_SIZE = 16;
const COLS = 38;
const RATIO = 9 / 16;
const WIDTH = COLS * TILE_SIZE;
const HEIGHT = WIDTH * RATIO;

export const CONFIGS = {
    TILE_SIZE,
    COLS,
    WIDTH,
    HEIGHT,
};
